import { Test } from '@nestjs/testing';
//import { AppModule } from '../../../../../../../apps/neox-api/src/app/app.module';

describe('TodoService Int', () => {
  beforeAll(async () => {
    const moduleRef = await Test.createTestingModule({
      //imports: [AppModule],
    }).compile();
  });
});
