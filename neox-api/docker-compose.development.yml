version: "3.8"

x-logging: &default-logging
  driver: json-file
  options:
    max-size: 100m
    max-file: 100

networks:
  neox-dev-network:
    driver: bridge

services:

  # DATABASE
  neox_dev_db:
    container_name: dev-neox-db
    image: bitnami/postgresql:latest
    platform: linux/amd64
    ports:
      - ${DB_PORT}:${DB_PORT}
    restart: always
    volumes:
      - neox-dev-db-data:/bitnami/postgresql
    environment:
      - POSTGRESQL_DATABASE=${DB_DATABASE}
      - POSTGRESQL_USERNAME=${DB_USERNAME}
      - POSTGRESQL_PASSWORD=${DB_PASSWORD}
    networks:
      - neox-dev-network
    logging: *default-logging

volumes:
  neox-dev-db-data:
